package org.haftrust.verifier.dao;

import org.haftrust.verifier.model.Fom;
import org.springframework.data.jpa.repository.JpaRepository;

/**
 *
 */
public interface FomDAO extends JpaRepository<Fom, Integer> {
}
